# AGENTS.md

## 目的
- `docs/plan.md` に沿って開発を進める
- 各タスクで抜け漏れがないかをセルフチェックする
- レール（契約/不変条件/構造）に従属した固定化を優先する

## 必須ルール（常に適用）
- 作業前に `docs/plan.md` を参照し、該当フェーズと未完了項目を特定する
- 作業後に「何を変更したか/何が未変更か」を明示する
- 変更がレールに影響する場合は、以下のセルフチェックを必ず通す
- 方針検討・再考・相談の段階では、ファイル編集/コマンド実行を行わない
- 変更を伴う操作は、ユーザーの明示的な実行指示（例: 「修正して」「実装して」「進めて」）を受けてから行う
- 指示が曖昧な場合は、実行前に確認質問を1回行い、回答確定まで待機する
- コメントは標準的で簡潔な説明に留め、冗長な装飾や意図の重複を避ける
- コメントは日本語で書く
- 開発はTDD（Red→Green→Refactor）を基本とし、仕様テストを先に書く
- テスト関数名は日本語混じり（例: `TestCreateOrder_正常系`）で統一する

## セルフチェック（毎タスク）
- 契約: `contracts/openapi.yaml` の単一参照点を維持しているか
- 差分: 破壊的変更がある場合、`contracts/migrations/` が追加されているか
- 生成: 生成物の整合が崩れていないか（再生成で差分が出ないか）
- 構造: 依存方向/越境ルールを破っていないか
- 不変条件: ドメインの性質を破っていないか
- 境界観測一貫性: 外形では検出できない互換性が崩れていないか

## plan.md監査プロトコル（抜け漏れ再発防止）
- 対象: `docs/plan.md` の抜け漏れ確認・順序整合確認・タスク修正
- 監査は2パスで行う（同じ観点を反復する）
- パス1（指摘抽出）: 編集せず、指摘のみ列挙する
- パス2（再監査）: 修正後に同一観点で再確認し、残件の有無を明示する
- 指摘抽出後、ユーザーの明示的な修正指示があるまで `plan.md` を編集しない
- 監査観点を毎回固定する
  - 重複: 同一責務のタスクが複数フェーズに重複していないか
  - 順序: 先行タスクなしでは着手できないタスクが先に来ているか
  - 責務: 「方針決定」と「実装作業」が混在していないか
  - 契約整合: OpenAPI/テスト方針/planの記述が矛盾していないか
  - CI接続: CI実装責務の所在が1フェーズに集約されているか
  - 粒度: 実行可能な最小単位までタスクが分解されているか
- 完了宣言条件
  - 上記観点で再監査し、指摘0件である
  - もしくは残課題を「未対応」として明示した上で、次の最小ステップを確定している

## 進行時の出力ルール
- 参照したフェーズと作業内容を短く書く
- 追加/変更/未対応の項目を箇条書きで示す
- 次にやるべき最小ステップを提案する
