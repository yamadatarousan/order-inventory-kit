#!/usr/bin/env bash
set -euo pipefail

# TODO: Replace with actual OpenAPI diff tool (e.g., oasdiff)
# Example:
# oasdiff -format text "$1" "$2"

echo "[openapi-diff] placeholder: no diff tool configured"
